<Window x:Class="Semana2Rudas.MantenimientoConductores"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Semana2Rudas"
        mc:Ignorable="d"
        Title="MantenimientoConductores" Height="450" Width="800">
    <Grid Margin="20">
        <DataGrid x:Name="datosConductores" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Peso Maximo" Binding="{Binding PesoMaximo}" Width="*"/>
                <DataGridTextColumn Header="Placa" Binding="{Binding Placa}" Width="*"/>
                <DataGridTextColumn Header="Peso Vacio" Binding="{Binding PesoVacio}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
